アルゴリズム
階層下のファイルを一つずつopen
１行ごとにread
show_message(を検索 ※show_message_onlyに注意
なければ次の行あれば,を検索 ※show_message(カメラマン,"")とかあるとエラー出そう
なければ次の行あれば(を検索
(,に挟まれたメンバ名抽出　両端空白改行除去 多分カメラマン対策でisalnum()
"の数が2なら中のメッセージ抽出※シングルクォート対応しないといけない？
"の数が1なら次の行にメッセージが続いているので次の行から"の数検索
"見つけたらその位置を記憶し+1の位置に,とメンバ名に対応するV000_0000挿入
result = src[:idx] + "/7/" + src[idx+3:]
